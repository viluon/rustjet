/*
 * RegioJet API
 *
 * A set of endpoints to interact with RegioJet transport services. Search for connections, book tickets, see the list of served stations and more. All endpoints consume and produce JSON strings, with the exception of ticket printing (/tickets/{ticketId}/print) that produce printable HTML code.
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: developers@studentagency.cz
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Timetable {
    /// Connection ID
    #[serde(rename = "connectionId")]
    pub connection_id: i64,
    /// Departure city
    #[serde(rename = "fromCityName")]
    pub from_city_name: String,
    /// Arrival city
    #[serde(rename = "toCityName")]
    pub to_city_name: String,
    /// Individual stations (including metadata) sort according to order which connection builds.
    #[serde(rename = "stations")]
    pub stations: Vec<models::TimetableStation>,
}

impl Timetable {
    pub fn new(
        connection_id: i64,
        from_city_name: String,
        to_city_name: String,
        stations: Vec<models::TimetableStation>,
    ) -> Timetable {
        Timetable {
            connection_id,
            from_city_name,
            to_city_name,
            stations,
        }
    }
}
